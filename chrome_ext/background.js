"use strict";

/**
 * Placeholder background service worker for future navigation interception.
 * TODO: Monitor URL loads and send risk assessments when prediction API is integrated.
 */
self.addEventListener("install", () => {
  // TODO: Initialize caches or prefetch configuration if required.
});

self.addEventListener("activate", () => {
  // TODO: Clean up old data stores when upgrading the extension.
});

// TODO: Add listeners (e.g., chrome.webNavigation) to trigger phishing checks.
